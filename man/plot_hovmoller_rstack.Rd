% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hovmoller_rstack.R
\name{plot_hovmoller_rstack}
\alias{plot_hovmoller_rstack}
\title{Hovmöller plot (Vertical section) from raster stack of continuous values layered
along line / transect across Z dimension}
\usage{
plot_hovmoller_rstack(
  rstack,
  tr,
  z_values,
  quiet = FALSE,
  visual_info = FALSE,
  pts_res = NULL,
  z_units = NULL,
  nx = NULL,
  ny = NULL,
  plot = TRUE,
  plot_relation = 1:1,
  colramp = viridis::viridis(100),
  colramp_breaks = NULL,
  val_range = NULL,
  rev = FALSE,
  contour = FALSE,
  contour_col = "black",
  contour_alpha = 0.4,
  contour_lwd = 0.5,
  margin_plot = "none",
  y_ticks = 5,
  x_ticks = 5,
  cex_y = 0.8,
  cex_x = 0.7,
  zlim = NULL,
  ...
)
}
\arguments{
\item{rstack}{A \code{terra::SpatRaster} or \code{raster::RasterStack} with multiple layers
representing different depths or altitudes.}

\item{tr}{An \code{sf} object (LINESTRING) representing the transect path.}

\item{z_values}{Numeric. Either a single value (equidistant spacing) or a
vector of length \code{nlyr(rstack)} specifying the depth/altitude/distance of each
raster stack layer (z value (coordinate).}

\item{quiet}{Logical. If \code{TRUE}, suppresses status messages. Default \code{FALSE}.}

\item{visual_info}{Logical. If \code{TRUE}, plots a 2D map showing the transect alignment
and quality control of the extent.}

\item{pts_res}{Numeric. Resolution factor for point sampling along the transect.
Default \code{NULL} (uses raster resolution). Higher values increase point density.}

\item{z_units}{Character. Label for the Z-axis (e.g., "Depth (m)" or "Altitude (m)").}

\item{nx}{Integer. Number of points in the X-axis for interpolation. Default: \code{nrow(pts)}.}

\item{ny}{Integer. Number of points in the Y-axis for interpolation.
Default, number of z values: \code{ny = max(z_values_final) / 1.1}.}

\item{plot}{Logical. If \code{TRUE}, displays the plot immediately in the enviroment.}

\item{plot_relation}{Numeric fraction of the aspect ratio of the final plot.
Default \code{1:1}, but it can be change based on user preferences.}

\item{colramp}{A color palette or vector of colors for the plot. Default \code{viridis::viridis(100)}.}

\item{colramp_breaks}{Numeric. Number of breaks for color ramp (this parameter is linked to contours)}

\item{val_range}{Vector. \code{NULL} by default, (1) calculate by min and max from stack values;
or (2) custom values range for plot e.g.: c(0,1).}

\item{rev}{Logical. If \code{FALSE} (default), reverses or not the Y-axis (useful for depth profiles).}

\item{contour}{Logical \code{FALSE} (default), If \code{TRUE}, adds contour lines to the plot, and
then change the next @param: contour_col, contour_alpha, contour_lwd.}

\item{contour_col}{Color of the contour lines. Default \code{"black"}.}

\item{contour_alpha}{Numeric (0-1). Transparency of contour lines.}

\item{contour_lwd}{Numeric. Line width of contours.}

\item{margin_plot}{Character. Internal margin plots to show. Default \code{"none"}, \code{"top"}, \code{"righ"}}

\item{y_ticks}{Numeric. Desired number of intervals for the Y-axis.}

\item{x_ticks}{Numeric. Number of label sticks in X axi for coordinates along}

\item{cex_y}{Numeric. Font size for label in Y axi.}

\item{cex_x}{Numeri. Font size for label in X axi.
The actual number of labels is dynamically optimized to provide "pretty" (rounded) values.}

\item{zlim}{Numeric vector \code{c(min, max)} for the Y-axis limits.}

\item{...}{Additional arguments passed to \code{rasterVis::levelplot}.}
}
\value{
A \code{trellis} object (from \code{lattice} via \code{rasterVis}).
}
\description{
\code{plot_hovmoller_rstack()} visualizes a vertical cross-section of spatial data
(SpatRaster/RasterStack) along a specific transect (sf LINESTRING). It interpolates
values between layers.
(e.g., Copernicus Marine Services environmental variables, 3D Habitat suitability, etc)
}
\details{
#' This function performs a 2D bilinear interpolation (using the akima package)
between the vertical layers of a raster stack to create a continuous
cross-section along a geographical transect.
\enumerate{
\item \strong{Spatial Alignment}: Synchronizes CRS between the transect and raster stack,
clipping the line to the data extent to ensure valid extraction.
\item \strong{Transect Discretization}: Segments the LINESTRING into equidistant points.
Sampling density is controlled by \code{pts_res} relative to the raster's
horizontal resolution.
\item \strong{Vertical Interpolation}: Extracts stack values at each point and generates
a "Distance vs. Depth" grid. It uses the \code{akima} algorithm to interpolate
between discrete vertical bins, creating a continuous cross-section.
\item \strong{Visual Rendering}: Uses \code{rasterVis::levelplot} for plotting, but pathlyXYZ
implemented label text with geographic coordinates (Lat/Lon) on the X-axis,
and choose the scale values for the color ramp.
}
}
\note{
This function uses the native R pipe \code{|>} and requires R version 4.1.0 or higher.
The function follows a streamlined workflow:
}
\examples{
\donttest{
library(terra)
library(sf)

# 1. Create a SpatRaster stack from 'volcano' dataset
r <- terra::rast(volcano)
# Set fake extent and CRS for spatial operations
terra::ext(r) <- c(-0.1, 0.1, -0.1, 0.1)
terra::crs(r) <- "EPSG:4326"

# Create a stack simulating 6 depth/altitude layers
rstack <- c(r, r * 0.85, r * 0.7, r * 0.5, r * 0.3, r * 0.1)
names(rstack) <- paste0("layer_", 1:6)

# 2. Create a transect line within the raster extent
p1 <- matrix(c(-0.08, -0.08), ncol = 2)
p2 <- matrix(c(0.08, 0.08), ncol = 2)
tr <- sf::st_linestring(rbind(p1, p2)) |>
  sf::st_sfc(crs = "EPSG:4326") |>
  sf::st_as_sf()

# 3. Run pathlyXYZ custom Hovmoller or Vertical section plot
p <- plot_hovmoller_rstack(
  rstack = rstack,
  tr = tr,
  z_values = 50,          # 50m between each rstack layer
  colramp_breaks = 20,
  rev = TRUE,             # Reverse Y-axis for depth
  z_units = "Depth (m)",
  contour = TRUE,
  contour_col = "black",
  x_ticks = 3,
  visual_info = TRUE,     # Show the QC map
  plot = TRUE
)
}
-----------------------------------------------------------------------------

}
